import { NumberShortPipe } from './number-short.pipe';

describe('NumberShortPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberShortPipe();
    expect(pipe).toBeTruthy();
  });
});
